export enum VariantType {
  primary = 'primary',
  secondary = 'secondary',
  ternary = 'ternary',
}

export type VariantTypes =
  | VariantType.primary
  | VariantType.secondary
  | VariantType.ternary;
